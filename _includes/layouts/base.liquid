<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    {% if page.url == "/" %}
        <title>Moss Cat</title>
    {% else %}
        <title>{{ title }} · Moss Cat</title>
    {% endif %}
    <meta name="description" content="{{ description | default: 'Moss Cat is an art collective that dreams of a gentle future. Nature-inspired art and jewelry that instills hope and a longing for a brighter world.' }}">
    <meta name="keywords" content="Moss Cat, jewelry, candles, nature inspired art, art collective, moss aesthetic">
    <meta name="author" content="Moss Cat">
    {% if page.url == "/" %}
        <meta property="og:title" content="Moss Cat">
    {% else %}
        <meta property="og:title" content="{{ title }} · Moss Cat">
    {% endif %}
    <meta property="og:description" content="{{ og_description | default: description }}">
    <meta property="og:url" content="https://mosscat.co">
    <meta property="og:type" content="website">
    <meta property="og:image" content="https://mosscat.co/og-image.png"> 
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/styles.css">
    <meta name="robots" content="noindex, nofollow">
</head>
<body class="mc-atmosphere">
    <div class="mc-tint"></div>
    <div class="mc-blur mc-blob-1"></div>
    <div class="mc-blur mc-blob-2"></div>
    <div class="mc-blur mc-blob-3"></div>

    <main>
        {{content}}
    </main>

    <script src="/script.js"></script>
</body>
</html>